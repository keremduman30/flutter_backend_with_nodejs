import 'package:flutter/cupertino.dart';

class NotFoundNavigatorWidget extends StatelessWidget {
  const NotFoundNavigatorWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("not found Page"),
    );
  }
}
